---
title: The validation model in `:context`
description: Deep dive into the language-agnostic validation model implemented as a Spine Bounded Context.
headline: Documentation
---

# The validation model in `:context`

> This page is a placeholder. Content is in progress.

This section will provide a deep dive into the language-agnostic validation model:
views, events, and reactions; how built-in options are translated into model state;
how custom options plug in via the `ValidationOption` SPI from the model side; and
the error-reporting conventions used inside the model.
